const host = '/';

export const routes = {
  main: () => host,
  profile: () => host + 'profile',
  login: () => host + 'login',
  forgot: () => host + 'forgot',
  singup: () => host + 'signup',
  policy: () => host + 'policy',
};
